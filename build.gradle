apply from: "./libraries.gradle"

group 'com.gary'
version '1.0-SNAPSHOT'

apply plugin: 'java'
apply plugin: 'war'

sourceCompatibility = 1.5

repositories {
    mavenCentral()
}

dependencies {
//    testCompile group: 'junit', name: 'junit', version: '4.11'
    compile(libraries.slf4j_api)
    compile(libraries.junit)
    compile(libraries.druid)
    compile(libraries.fastjson)
    compile(libraries.commons_lang3)
    compile(libraries.commons_net)
    compile(libraries.httpclient)
    compile(libraries.im4java)
    compile(libraries.spring_context_support)
    compile(libraries.spring_webmvc)
    compile(libraries.spring_jdbc)
    compile(libraries.spring_context)
    compile(libraries.spring_data_redis)
    compile(libraries.servlet_api)
    compile(libraries.jsoup)
    compile(libraries.guava)
    compile(libraries.slf4j_api)
//    compile(libraries.excel_mapping)
    compile(libraries.poi)
    compile(libraries.javacsv)
    compile(libraries.xmemcached)
    compile(libraries.gson)

}
